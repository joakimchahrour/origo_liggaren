# Origo Liggaren

Welcome to the Github repository for Origo Liggaren. Liggaren is written in typst with it's native HTML export support and functions. You can either use the typst web app or download the compiler. 

In order to connect the website to Google Sheets Google's own apps script application was used. 

Working on: (RESOLVED)
* Right now it the website might look frozen when the submit button is hit. Users hit multiple times and it creates dublets in the sheet. Something should be added so that one does not thing that the loader is frozen and something that doesn't allow the user to hit submit while the site is working.
* Users are entering their entire umu mail instead of their umu-id. Add an exmple of what to write so that the used knows.
* Some users are writing umu-id with capital letters and some aren't. Need for normalisation, it should all be small letters no matter what the user inputs.
